# Dependencies
/node_modules
/.pnp
.pnp.js

# Testing
/coverage

# Next.js build output
/.next
/out

# Build artifacts
/.turbo
/dist

# Static files cache (e.g., SWC, ESLint cache)
/.cache
.next/cache

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Operating system files
.DS_Store
Thumbs.db

# Editor-specific files
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment files (sensitive data)
/.env
/.env.*      # This includes .env.local, .env.production, etc.
/.env.local
.env.*.local

# Lockfiles (optional, if you prefer regenerating them)
/yarn.lock
/pnpm-lock.yaml

# Optional: Ignore the `.vercel` folder if deploying to Vercel
.vercel

# Optional: Ignore the `.firebase` folder if using Firebase
.firebase

# Optional: Ignore specific build tools outputs
.eslintcache
.prettierignore
